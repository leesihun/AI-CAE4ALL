# CPU-only inference bundle dependencies. Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# No CUDA, no torch_cluster (see README.md "Why CPU-only"). The versions below
# are pinned to what was actually built and parity-tested for this bundle
# (see README.md "Verification"); >= floors are the minimum believed-compatible
# version, not a promise every newer release keeps the same checkpoint-loading
# behavior. Re-run the parity checks before moving off the pinned versions.

torch==2.13.0        # >=2.2
torch_geometric==2.8.0.post1  # >=2.5
scipy==1.15.3         # >=1.11
h5py==3.16.0          # >=3.10
numpy==2.2.6          # >=1.24
scikit-image==0.25.2  # >=0.22
trimesh==4.12.2       # >=4.0
