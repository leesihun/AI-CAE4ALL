model	MeshGraphNets
mode	inference  # Train / Inference
gpu_ids	0  # ex2 inference for ablation arm p12
log_file_dir	../../output/meshgraphnets/ex2/infer_himgn_p12.log
modelpath	../../output/meshgraphnets/ex2/model_himgn_p12.pth
%   INFERENCE for ablation arm p12 (ex2).
%
%   GENERATED by gen_ablation_arms.py from config_infer_himgn.txt +
%   config_train_himgn_p12.txt -- do not edit; re-run the generator.
%
%   Architecture keys below are informational: inference_profiles/rollout.py
%   overwrites them from the checkpoint's `model_config` at load time, which
%   is what actually determines the branch layout and pooling operators. They
%   are written here anyway so the file states what checkpoint it expects.
%   The hierarchy IS rebuilt per sample at inference exactly as in training,
%   including the K parallel partitions -- skip_projs is sized for (1 + K)
%   merge inputs, so a mismatch here d oes not silently degrade, it fails to
%   multiply.
dataset_dir	../../dataset/ex2.h5
infer_dataset	../../dataset/ex2_infer.h5  # held out from training
inference_output_dir	../../output/meshgraphnets/rollout/ex2/model_himgn_p12
infer_timesteps	49  # autoregressive rollout steps from t=0
%   Common params (architecture overridden by checkpoint model_config at load)
input_var	4  # mirrored from config_train_himgn_p12.txt
output_var	4  # mirrored from config_train_himgn_p12.txt
feature_loss_weights	1.0, 1.0, 1.0, 1.0  # Per-feature loss weights for [x_disp, y_disp, z_disp, stress]
edge_var	8  # mirrored from config_train_himgn_p12.txt
positional_features	4  # mirrored from config_train_himgn_p12.txt
'
%   Network parameters (overridden by checkpoint)
message_passing_num	15
Training_epochs	1
Batch_size	1
LearningR	0.0001
Latent_dim	128  # mirrored from config_train_himgn_p12.txt
num_workers	2
std_noise	0.0  # input-noise injection for autoregressive stability (multi-step)
weight_decay	0.0001  # AdamW decoupled weight decay (decimal form only)
augment_geometry	False  # OFF for ex2: directional y-sag breaks the Z-rotation/reflection isotropy assumption
grad_accum_steps	1
'
%   Memory Optimization
use_checkpointing	False
'
%   Performance Optimization
use_amp	True  # Mixed precision training with bfloat16
use_ema	True  # EMA shadow model for validation/inference
ema_decay	0.99
test_interval	100  # Run test/visualization every N epochs
val_interval	1  # Run val eval every N epochs
'
%   Node Type Parameters
use_node_types	True  # mirrored from config_train_himgn_p12.txt
'
%   World Edge Parameters
use_world_edges	True  # mirrored from config_train_himgn_p12.txt
'
%   Test set control
test_batch_idx	0
plot_feature_idx	-1  # Feature index to visualize in plots (-1 = last feature, i.e., stress)
'
%   Multi-Scale (overridden by checkpoint)
use_multiscale	True  # mirrored from config_train_himgn_p12.txt
coarsening_type	voronoi_seedmean  # mirrored from config_train_himgn_p12.txt
voronoi_clusters	5000, 100  # mirrored from config_train_himgn_p12.txt
multiscale_levels	2  # mirrored from config_train_himgn_p12.txt
mp_per_level	4, 6, 8, 6, 4  # mirrored from config_train_himgn_p12.txt

'
%   Arm-specific keys absent from the template (checkpoint still wins):
voronoi_branches	1, 4  # mirrored from config_train_himgn_p12.txt
pool_type	attention  # mirrored from config_train_himgn_p12.txt
unpool_type	attention  # mirrored from config_train_himgn_p12.txt
pool_heads	4  # mirrored from config_train_himgn_p12.txt