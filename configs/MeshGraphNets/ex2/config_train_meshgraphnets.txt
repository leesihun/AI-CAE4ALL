model	MeshGraphNets
mode	train  # Train / Inference
gpu_ids	0  # ex2 3D transient regular/vanilla (20 MP) baseline
parallel_mode	ddp
log_file_dir	../../output/meshgraphnets/ex2/train_vanilla.log
modelpath	../output/meshgraphnets/ex2/model_vanilla.pth
%   Datasets (ex2.h5: 50 samples x 50 timesteps, 3D mesh -> multi-step delta training;
%   flat/non-hierarchical baseline -- HI-MGN uses config_train_himgn.txt instead)
dataset_dir	../dataset/ex2.h5
infer_dataset	../dataset/ex2.h5
infer_timesteps	49  # 50 stored timesteps -> 49 autoregressive rollout steps
split_seed	42
%   Common params
input_var	4  # number of input variables: x_disp, y_disp, z_disp, stress (excluding node types)
output_var	4  # number of output variables: x_disp, y_disp, z_disp, stress (z_disp is REAL for ex2)
feature_loss_weights	1.0, 1.0, 1.0, 1.0  # Per-feature loss weights for [x_disp, y_disp, z_disp, stress]
edge_var	8  # deformed dx/dy/dz/dist + reference dx/dy/dz/dist
positional_features	4  # Rotation-invariant node features: [centroid_dist, mean_edge_len, + encoding]
'
%   Network parameters
message_passing_num	15  # Flat processor blocks (vanilla MeshGraphNets)
Training_epochs	500
Batch_size	4
LearningR	0.0001
Latent_dim	128  # MeshGraphNets latent dimension
num_workers	12  # pin_memory off, so workers are the real limit now
prefetch_factor	2  # flat batches are ~350MB; 8x4 prefetch would pin ~11GB
pin_memory	False  # 0.5GB variable-shape batches: pinning serializes in the parent (0.67s/batch vs 0.26s off)
std_noise	0.01  # input-noise injection for autoregressive stability (multi-step)
weight_decay	0.0001  # AdamW decoupled weight decay (decimal form only)
augment_geometry	True  # OFF for ex2: directional y-sag breaks the Z-rotation/reflection isotropy assumption
grad_accum_steps	1
'
%   Memory Optimization
use_checkpointing	True
'
%   Performance Optimization
use_amp	True  # Mixed precision training with bfloat16
use_ema	True  # EMA shadow model for validation/inference
ema_decay	0.99
test_interval	10  # Run test/visualization every N epochs
val_interval	5  # Run val eval every N epochs
'
%   Node Type Parameters
use_node_types	True  # Add one-hot encoded node types to node features
'
%   World Edge Parameters
use_world_edges	True
'
%   Test set control
test_batch_idx	0, 10, 20, 30, 40, 100, 200, 500, 1000
plot_feature_idx	-1  # Feature index to visualize in plots (-1 = last feature, i.e., stress)
'
%   Multi-Scale / Hierarchical Parameters -- OFF for the regular/vanilla baseline
use_multiscale	False
