# parallelism package: deterministic pipeline model-split (parallel_mode=model_split),
# ported from MeshGraphNets' parallelism/ package and adapted to the grid-latent
# operator models (fno, gino). DeepONet/Point-DeepONet have no sequential latent
# stack and are rejected at config validation.
